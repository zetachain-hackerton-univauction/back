# ?? ??
server.port=${PORT:8080}

# PostgreSQL DB
spring.datasource.url=${jdbc:postgresql://dpg-d2kvjp75r7bs73d5in1g-a.internal:5432/DBNAME?sslmode=require}
spring.datasource.username=${univection_user}
spring.datasource.password=${QQy5iMrujxZaEdpWSvUCQTmzthAbwb7P}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.profiles.active=${SPRING_PROFILES_ACTIVE:prod}